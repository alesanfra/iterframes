[package]
name = "iterframes"
edition = "2021"

[dependencies]
pyo3 = { version = "*", features = ["extension-module", "abi3-py39"] }
ffmpeg = { package = "ffmpeg-next", version = "=7.1.0" }
crossbeam = "*"

[lib]
name = "iterframes"
crate-type = ["cdylib"]
